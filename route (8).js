import{NextResponse}from"next/server";import{sql}from"@/lib/db";import{requireUser}from"@/lib/auth";export async function POST(){try{const u=await requireUser();if(u.id==="jiwooeo"||u.subadmin)return NextResponse.json({error:"관리자/부관리자는 요청할 수 없어요."},{status:400});await sql`INSERT INTO admin_requests(user_id)VALUES(${u.id})ON CONFLICT(user_id)DO NOTHING`;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:500})}}