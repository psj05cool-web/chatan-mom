import{NextResponse}from"next/server";import{sql}from"@/lib/db";import{requireUser}from"@/lib/auth";export async function DELETE(req,{params}){try{const u=await requireUser();if(u.id!=="jiwooeo")return NextResponse.json({error:"관리자만 삭제할 수 있어요."},{status:403});const{id}=await params;await sql`UPDATE users SET subadmin=false WHERE id=${id}`;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:500})}}