import{NextResponse}from"next/server";import{sql,initDb}from"@/lib/db";import{requireUser}from"@/lib/auth";export async function POST(req){try{await initDb();const u=await requireUser();if(u.id!=="jiwooeo"&&!u.subadmin)return NextResponse.json({error:"관리자와 부관리자만 공지를 작성할 수 있어요."},{status:403});const b=await req.json();if(!b.title||!b.body)return NextResponse.json({error:"제목과 내용을 입력해 주세요."},{status:400});await sql`INSERT INTO notices(title,body,image,author_id)VALUES(${String(b.title).slice(0,150)},${String(b.body).slice(0,10000)},${b.image||null},${u.id})`;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:500})}}