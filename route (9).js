import{NextResponse}from"next/server";import{sql}from"@/lib/db";import{requireUser}from"@/lib/auth";export async function PATCH(req,{params}){try{const u=await requireUser();if(u.id!=="jiwooeo")return NextResponse.json({error:"관리자만 처리할 수 있어요."},{status:403});const{id}=await params,b=await req.json();if(b.approve)await sql`UPDATE users SET subadmin=true WHERE id=${id}`;await sql`DELETE FROM admin_requests WHERE user_id=${id}`;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:500})}}