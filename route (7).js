import{NextResponse}from"next/server";import{sql}from"@/lib/db";import{requireUser}from"@/lib/auth";export async function DELETE(req,{params}){try{const u=await requireUser(),{id}=await params,r=await sql`SELECT author_id FROM posts WHERE id=${id}`;if(!r[0])return NextResponse.json({error:"게시물을 찾을 수 없어요."},{status:404});if(r[0].author_id!==u.id&&u.id!=="jiwooeo")return NextResponse.json({error:"삭제 권한이 없어요."},{status:403});await sql`DELETE FROM posts WHERE id=${id}`;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:500})}}